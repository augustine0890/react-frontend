name: Deploy to Github

on:
  push:
    branches:
      - master
      - dev
    
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
      uses: actions/checkout@v2
      run: docker build . --file Dockerfile